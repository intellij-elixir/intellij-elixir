# Generated by generate.exs; do not edit. Regenerate with `mise exec -- elixir generate.exs` in testData/org/elixir_lang/code_insight/code_intelligence_matrix.
defmodule Src.Defmacrop.XReceive do
  receive do
    :matrix_never_sent -> nil
  after
    0 ->
    defmacrop src_defmacrop_x_receive_snoc(q, x), do: quote(do: {unquote(q), unquote(x)})
  end

  def local_site(a, b), do: src_defmacrop_x_receive_snoc(a, b) # @local

  def uses(a, b) do
    {
      a,
      b
    }
  end
end
